import React from 'react';
import Base from '../Base';

const Auth = () => {
    return (
        <Base>
        <div>
            <h1>Auth</h1>
        </div>
        </Base>
    );
};

export default Auth;